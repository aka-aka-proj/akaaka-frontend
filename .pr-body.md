## Summary

- Reconcile `preview` ancestry with the current `main` release history.
- Preserve the existing identical application tree; this is a history-only merge.

## Verification

- `git diff --quiet origin/main HEAD`
- `git merge-base --is-ancestor origin/main HEAD`
- `git rev-parse HEAD^{tree}` equals `origin/main^{tree}`

Consumes-Expand: none
